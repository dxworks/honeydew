name: Honeydew
run: onEach
results:
  - dir: ${instrument}/results
    files:
      - "*.json"
commands:
  - name: run honeydew
    win: honeydew.exe extract ${repo} results/model.json
    unix: honeydew extract ${repo} results/model.json

