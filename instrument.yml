name: Honeydew

results:
  - dir: ${instrument}/results

commands:
  - name: run honeydew
    win: .\bin\win\Honeydew.exe extract ${repo} ${options}
    unix: ./bin/linux/Honeydew extract ${repo} ${options}

parameters:
  options: "--disable-progress-bars"

